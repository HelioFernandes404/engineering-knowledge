{
    "compilerOptions": {
        "resolveJsonModule": true,
        "baseUrl": ".", // This must be specified if "paths" is.
        "paths": {
            "@src/*": ["src/*"], // This mapping is relative to "baseUrl".
            "@pages/*": ["tests/pages/*"] // This mapping is relative to "baseUrl".
        }
    }
}
